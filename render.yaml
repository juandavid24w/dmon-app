databases:
    - name: mysite
      databaseName: mysite
      user: mysite

services:
    - type: web
      name: polls-app
      env: python
      buildCommand: './build.sh'
      startCommand: 'gunicorn django_project.wsgi:application'
      autoDeploy: false
      envVars:
          - key: DATABASE_URL
            fromDatabase:
                name: mysite
                property: connectionString
          - key: SECRET_KEY
            generateValue: true
          - key: WEB_CONCURRENCY
            value: 4
